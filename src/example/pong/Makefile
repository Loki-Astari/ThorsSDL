THORSANVIL_ROOT             = $(realpath ../../../)

LINK_LIBS					+= ThorsUI ThorsGraphics ThorsWidgets
CXXFLAGS					+= $(SDL_CFLAGS)
LDLIBS						+= $(SDL_LIBS)
TARGET                      = pong.app
CONFIG						= data

include $(THORSANVIL_ROOT)/build/tools/Makefile
